apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "nginx-test"
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://bjw-s-labs.github.io/helm-charts
      chart: app-template
      targetRevision: 3.7.3
      helm:
        valueFiles:
          - $values/kubernetes/apps/onprem-prod/nginx-test/values.yaml
    - repoURL: https://github.com/bearylabs/operations.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: "nginx-test"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
